#!/bin/bash

#this script demonstate the usage of
#grep -- a command line utility
# in Bash for searching text

grep "abc" textfile
#grep -i "aBc" textfile
echo

#grep 'abc' textfile
#grep -i "aBc" textfile
#echo

#grep abc textfile
#grep -i abc textfile
echo
